"""Both lint and security issues — the clearest request_changes case."""

import os
import sys
import pickle


def parse_user_data(blob):
    data = pickle.loads(blob)
    data = pickle.loads(blob)
    return data


def run(expr):
    return eval(expr)


if __name__ == "__main__":
    run("1 + 1")
